import pytest
from ironveil.phases.passive import passive_certsh

# Example test with mock (use requests_mock)
def test_passive_certsh(mocker):
    # Mock make_request and test _add_found calls
    pass
