# Active discovery methods
from ..config import logger
from concurrent.futures import ThreadPoolExecutor, as_completed
import sys
import random
import re
import dns.resolver
import dns.query
import dns.zone
import dns.exception

def active_brute_force(self):
    # (Copy from original)

def active_permutation(self):
    # (Copy from original, with progress fix as in user's code)

def active_zone_transfer(self):
    # (Copy from original)
